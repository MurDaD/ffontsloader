import { FontParser } from './fontParser';describe('FontParser', () => {  let fontParser;  beforeEach(() => {    fontParser = new FontParser([      'Test Font:300,700:latin,greek',      'AnotherTestFont:300,400,700',      'Just a Font',      'Font:latin',    ]);  });  it('parsing font works as expected', () => {    fontParser.parse();    const parsed = fontParser.getFonts();    expect(parsed).toEqual([      {        family: "Test Font",        variation: "n3",      },      {        family: "Test Font",        variation: "n7",      },      {        family: "AnotherTestFont",        variation: "n3",      },      {        family: "AnotherTestFont",        variation: "n4",      },      {        family: "AnotherTestFont",        variation: "n7",      },      {        family: "Just a Font",        variation: "n4",      },      {        "family": "Font",        "variation": "n4",      },    ]);  });  it('getting test string works as expected', () => {    expect(fontParser.getFontTestStrings()).toEqual({});  });});